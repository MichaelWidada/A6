<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Register</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body tabindex="-1">
  <%- include('partials/navbar', {page: '/register'}) %>

  <br />

  <div class="container mx-auto">
    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md w-full">
          <h1 class="text-5xl font-bold mb-4">Register</h1>

          <% if (successMessage) { %>
            <div class="alert alert-success mb-4">
              <span><%= successMessage %></span>
            </div>
            <a href="/login" class="btn btn-success w-full">Proceed to Login</a>
          <% } else { %>
            <% if (errorMessage) { %>
              <div class="alert alert-error mb-4">
                <span><%= errorMessage %></span>
              </div>
            <% } %>

            <form action="/register" method="POST" class="form-control gap-4">
              <input class="input input-bordered w-full" type="text" name="userName" placeholder="User Name" value="<%= typeof userName !== 'undefined' ? userName : '' %>" required />
              <input class="input input-bordered w-full" type="password" name="password" placeholder="Password" required />
              <input class="input input-bordered w-full" type="password" name="password2" placeholder="Confirm Password" required />
              <input class="input input-bordered w-full" type="email" name="email" placeholder="Email Address" required />
              <button class="btn btn-primary w-full">Register</button>
            </form>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
