<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Login</title>
  <link rel="stylesheet" href="/css/main.css" />
</head>

<body tabindex="-1">
  <%- include('partials/navbar', {page: '/login'}) %>

  <br />

  <div class="container mx-auto">
    <div class="hero bg-base-200 rounded-lg p-6">
      <div class="hero-content text-center">
        <div class="max-w-md w-full">
          <h1 class="text-5xl font-bold mb-4">Login</h1>

          <% if (errorMessage) { %>
            <div class="alert alert-error mb-4">
              <span><%= errorMessage %></span>
            </div>
          <% } %>

          <form action="/login" method="POST" class="form-control gap-4">
            <input class="input input-bordered w-full" type="text" name="userName" placeholder="User Name" value="<%= typeof userName !== 'undefined' ? userName : '' %>" required />
            <input class="input input-bordered w-full" type="password" name="password" placeholder="Password" required />
            <button class="btn btn-primary w-full">Login</button>
          </form>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
